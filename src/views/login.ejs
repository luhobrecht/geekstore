<!DOCTYPE html>
<html lang="es">
    <%- include("partials/head.ejs") %>
    <link rel="stylesheet" href="/css/login.css">
    <title>Iniciá sesión en Geek Store</title>
<body>
    <%- include("partials/header.ejs") %>
<main>
<form action="/" method="GET">
    <nav>
    <h2>Inicía sesión</h2>
    <div class="loginU">
            <input placeholder="USUARIO" type="text" name="user" id="user" value="<%= locals.old && old.user ? old.user: ""%>" required></p>   
            <% if (locals.errors && errors.user) {%>
                <p id="errores">
                    <%= errors.user.msg %> 
                </p>   
                <% } %>                 
            <input placeholder="CONTRASEÑA" type="password" name="password" id="password" required></p>
            <% if (locals.errors && errors.password) {%>
                <p id="errores">
                    <%= errors.password.msg %> 
                </p>   
                <% } %>  
    </div>
    <div id="recordame">
        <label for = "recordame">Recordame</label>
        <input type="checkbox" name="recordame" class="recordame">
        </div>
    <div class="submitMasEnviar">          
            <input type="submit" name="submit" id="enviar" value="ENVIAR"> </input>            
    </div>
    <button id="boton"><a href="/usuarios/registro">¿Todavía no tenes una cuenta? Registrate acá.</a></button>
    </nav>
</form>
<a href="/"><button id="vuelvo">Volver a Pagina Principal</button></a>       
</main>
<%- include("partials/footer.ejs") %>
    </body>
    </html>
